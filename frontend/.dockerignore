# Common ignores
node_modules
npm-debug.log
yarn-error.log
.env
.env.*
!.env.example

# Git specific
.git
.gitignore

# Build output - The Dockerfile for frontend creates this INSIDE the container,
# so it doesn't need to be ignored for COPY . . if it's generated by a previous step.
# However, if a local build occurs, it's good to ignore it.
/dist
/.vite

# Operating System specific
.DS_Store

# Dockerfile is needed by the build context, so it is NOT ignored.
# nginx.conf was previously here but now deleted as it's managed globally.
# Any other specific files or directories to ignore for the frontend build
# e.g., /tests
# e.g., /docs
